rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /posts/{imageId} {
      // Allow read access to anyone, since images are part of public posts
      allow read;
      
      // Allow write (create, update, delete) only for authenticated users
      allow write: if request.auth != null;
    }
  }
}
